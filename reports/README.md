# Reports

Training scripts write model scores and evaluation results here.

Expected outputs:

- `results.csv`
- `combined_scores.csv`
- `figures/`
- per-model score CSVs

Do not manually type model metrics into the README. Generate them from the pipeline so the portfolio remains reproducible.
